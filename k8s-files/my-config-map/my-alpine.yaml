apiVersion: v1
kind: Pod
metadata:
  name: demo-cmd
  labels:
    purpose: demo-cmd
spec:
  containers:
    - name: alpine
      image: alpine:latest
      command: [ "/bin/sh", "-c", "--" ]
      args: [ "while true; do sleep 30; done;" ]
  restartPolicy: OnFailure
